/// Constants used throughout the theme system
class ThemeConstants {
  /// Storage key for persisting the user's theme mode preference
  static const String themeModeKey = 'app_theme_mode';

  /// Default extra spacing for light theme
  static const double defaultExtraSpacing = 24.0;

  /// Default extra spacing for dark theme (typically larger for visual comfort)
  static const double defaultExtraSpacingDark = 32.0;

  ThemeConstants._(); // Private constructor to prevent instantiation
}
